{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - {{ "title.onboarding.edit"|trans }} - {{ note.name }}{% endblock %}

{% block body %}
<main role="main" class="content container">
    {{ include('_components/project_tabs.html.twig') }}

    <h1 class="mt-2">{{ "title.onboarding.edit"|trans }}</h1>

    {% for error in errors %}
        <div class="alert alert-danger">
            {{ error.messageKey|trans(error.messageData, 'security') }}
        </div>
    {% endfor %}

    {{ form_start(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.category) }}
    {{ form_row(form.text) }}

    <div class="clearfix">
        <div class="btn-group float-right" role="group" aria-label="">
            <a class="btn btn-secondary" href="{{ path('onboarding_show', {'id':note.id}) }}" role="button">{{ "link.onboarding.back"|trans }}</a>
            <button type="submit" class="btn btn-success">{{ "button.onboarding.save"|trans }}</button>
        </div>
        {{ form_end(form) }}

        {{ include('onboarding/_delete_form.html.twig') }}
    </div>
</main>
{% endblock %}
