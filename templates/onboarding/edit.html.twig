{% extends 'base.html.twig' %}

{% block title %}{{ "title.onboarding.edit"|trans }} - {{ note.name }}{% endblock %}

{% block body %}
<main role="main" class="container">
    {{ include('_components/project_tabs.html.twig') }}

    <div>
        <h1 class="mt-2">{{ "title.onboarding.edit"|trans }}</h1>

        {% for error in errors %}
            <div class="alert alert-danger">
                {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endfor %}

        {{ include('onboarding/_form.html.twig', {
            button_text: 'button.onboarding.save'
        }) }}
        {{ include('onboarding/_delete_form.html.twig') }}
    </div>
</main>
{% endblock %}
