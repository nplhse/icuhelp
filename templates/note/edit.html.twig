{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - {{ "title.note.edit"|trans }} - {{ note.name }}{% endblock %}

{% block body %}
    {{ include('_components/project_tabs.html.twig') }}

    <h1 class="mt-2">{{ "title.note.edit"|trans }}</h1>

    {% for error in errors %}
        <div class="alert alert-danger">
            {{ error }}
        </div>
    {% endfor %}

    {{ include('note/_form.html.twig', {'action': 'create'}) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    {{ encore_entry_script_tags('form-tag') }}
{% endblock %}